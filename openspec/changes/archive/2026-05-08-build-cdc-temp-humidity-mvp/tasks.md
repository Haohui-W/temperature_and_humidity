## 1. 工程结构与基础模型

- [x] 1.1 创建 `ui`、`measurement`、`reporting`、`storage`、`privacy`、`model` 包，并保持单 `:app` 模块和 XML/ViewBinding 架构。
- [x] 1.2 定义测量领域模型：测量状态、估算来源、估算结果、融合结果、质控结果和测量错误原因。
- [x] 1.3 定义报告领域模型：报告草稿、报告记录、报告状态、复制文本模型和报告保存错误。
- [x] 1.4 对齐 `minSdk` 与技术方案要求，确认是否从 API 24 调整到 API 26。

## 2. 权限与隐私基础

- [x] 2.1 在 `AndroidManifest.xml` 声明麦克风权限和必要的可选硬件特性。
- [x] 2.2 实现麦克风权限用途说明和运行时权限请求流程。
- [x] 2.3 实现麦克风拒绝后的热特征降级入口和用户可见提示。
- [x] 2.4 封装测量音频缓冲区清理逻辑，确保测量完成或失败后释放音频资源。
- [x] 2.5 增加点位语音识别隐私提示，并提供跳过语音识别改用手动输入的路径。

## 3. 环境测量能力

- [x] 3.1 实现测量会话状态机，覆盖空闲、权限检查、采集、估算、质控、成功、降级成功和失败状态。
- [x] 3.2 实现声学采集接口和声学估算器边界，返回声学温度、湿度、置信度、输入质量和不可用原因。
- [x] 3.3 实现热特征采集接口和热特征估算器边界，读取电池温度、CPU 温度、CPU 负载并处理缺失输入。
- [x] 3.4 实现融合引擎，按可用估算来源和置信度输出最终温度、湿度、总体置信度和来源说明。
- [x] 3.5 实现质控规则，阻止异常温湿度、低置信度或输入质量不合格的结果进入报告流程。
- [x] 3.6 实现测量失败和降级原因的结构化输出，供 UI 和报告摘要展示。

## 4. 本地报告与复制结果

- [x] 4.1 实现报告草稿创建逻辑，要求测量结果通过质控且点位名称非空。
- [x] 4.2 实现本地报告存储，保存报告字段、状态、创建时间、更新时间和必要来源摘要。
- [x] 4.3 使用 Android Keystore 或等效平台能力保护密钥，并加密本地报告敏感字段。
- [x] 4.4 实现报告历史查询和单条报告详情读取。
- [x] 4.5 实现 `CopyTextBuilder`，生成包含点位、温度、湿度、测量时间和质控说明的稳定复制文本。
- [x] 4.6 实现复制到剪贴板操作，并将报告状态从“已保存”更新为“已复制”。
- [x] 4.7 确认 MVP 不发起网络提交，不展示“已提交”状态，不自动填充第三方系统。

## 5. UI 流程

- [x] 5.1 替换模板首页为测量主界面，展示测量按钮、当前状态、权限/降级提示和最近报告入口。
- [x] 5.2 实现测量结果确认界面，展示温度、湿度、置信度、测量时间、估算来源和质控说明。
- [x] 5.3 实现点位名称输入区域，支持语音识别填入和手动编辑。
- [x] 5.4 实现保存报告操作，处理点位为空、本地保存失败和保存成功反馈。
- [x] 5.5 实现报告历史界面，展示点位、温湿度、创建时间和报告状态。
- [x] 5.6 实现报告详情和复制结果界面，展示完整报告字段、复制文本和复制操作反馈。

## 6. 测试与验证

- [x] 6.1 为融合引擎增加单元测试，覆盖双来源、单来源降级和无有效来源。
- [x] 6.2 为质控规则增加单元测试，覆盖通过、异常范围、低置信度和输入质量失败。
- [x] 6.3 为报告草稿、报告状态流转和复制文本生成增加单元测试。
- [x] 6.4 使用 Android Studio 连接真机 `Xiaomi 25113PN0EC` 验证应用启动、一键测量、权限授权路径和测量状态展示；模拟器异常时改用真机验证。
- [x] 6.5 使用真机运行结果和 UI 代码路径复核测量结果确认、点位输入、报告保存、历史查看和复制到剪贴板；MVP 暂不接入 API，仅生成待复制结果。
- [x] 6.6 为本地报告加密存取增加测试或调试验证步骤，确认不保存原始音频。
- [x] 6.7 运行 `./gradlew :app:assembleDebug`，构建成功；单元测试本轮按要求暂不作为阻塞项；沙箱首次访问 Gradle wrapper 锁文件受限后授权重跑成功。
